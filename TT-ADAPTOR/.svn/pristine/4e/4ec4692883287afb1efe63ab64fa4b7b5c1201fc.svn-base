package com.tts.reuters.adapter.utils.ric;

public interface IRicCodeProvider {

	/**
	 * Reload all settings
	 */
	public void reload();
	
	/**
	 * Retrieve the RIC code for a particular symbol + tenor
	 * 
	 * @param symbol
	 * @param tenor
	 * @return RIC code
	 */
	public String getRicCode(String symbol, String tenor);
}
